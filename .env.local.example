VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Build-time only (read by scripts/generate-seo-files.mjs, not by the app itself) — the site's
# real deployed origin, no trailing slash. Used for sitemap.xml/robots.txt URLs.
SITE_URL=https://your-domain.vercel.app
